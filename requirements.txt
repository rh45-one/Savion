# Keep direct runtime dependencies reproducible. Update these pins deliberately
# and test a rebuilt container before deploying it.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.3           # no [standard] extras → avoids uvloop/httptools builds
jinja2==3.1.6
python-multipart==0.0.32  # required for form/file uploads in FastAPI
