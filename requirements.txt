# Keep deps minimal and pure-Python to avoid compiling on Alpine
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0        # no [standard] extras → avoids uvloop/httptools builds
jinja2>=3.1,<4.0
python-multipart>=0.0.9   # required for form/file uploads in FastAPI
